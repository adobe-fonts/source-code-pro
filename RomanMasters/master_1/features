include (../../familyTables.fea);

table OS/2 {
	include (../../Roman/Regular/os2.fea);
	include (../../Roman/familyUnicodeRange.fea);
	include (../../familyOS2.fea);
} OS/2;

include (../../Roman/familyGSUB.fea);

# GPOS -----------------------------------
include (../../Roman/Regular/markclasses.fea);

feature mark {
	include (../../familyWidthsAdjust.fea);
	include (../../Roman/Regular/mark.fea);
} mark;


feature mkmk {
	include (../../Roman/Regular/mkmk.fea);
} mkmk;
