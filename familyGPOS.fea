# inlcuded by /SourceCodePro/features.fea

include (../markclasses.fea);

feature mark {
	include (../../../../familyWidthsAdjust.fea);
	include (../mark.fea);
} mark;


feature mkmk {
	include (../mkmk.fea);
} mkmk;
