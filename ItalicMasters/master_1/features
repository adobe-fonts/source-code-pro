include (../../familyTables.fea);

table OS/2 {
	include (../../Italic/It/os2.fea);
	include (../../Italic/familyUnicodeRange.fea);
	include (../../familyOS2.fea);
} OS/2;

include (../../Italic/familyGSUB.fea);

# GPOS -----------------------------------
include (../../Italic/It/markclasses.fea);

feature mark {
	include (../../familyWidthsAdjust.fea);
	include (../../Italic/It/mark.fea);
} mark;


feature mkmk {
	include (../../Italic/It/mkmk.fea);
} mkmk;
